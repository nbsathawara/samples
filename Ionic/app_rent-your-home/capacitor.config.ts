import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nbs.ionic.renthome',
  appName: 'Rent Your Home',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
